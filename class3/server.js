const express = require("express");
const nodemailer = require("nodemailer")

const app =  express()

app.post("/send-mail" , async (req , res )=>{
    try{

    }
    catch{
        
    }
});